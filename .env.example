# .env.example
#
# Copy this file to .env and fill in your values before running Docker.
# .env is gitignored — never commit your actual secrets.
#
#   cp .env.example .env
#   # then edit .env with your values

# ── OpenClaw Gateway ─────────────────────────────────────────────────────────

# Generated automatically by docker-setup.sh (from the official openclaw repo).
# Find it in ~/.openclaw/config after first setup, or run:
#   docker compose run --rm openclaw-cli config show
OPENCLAW_GATEWAY_TOKEN=


# ── Telegram ──────────────────────────────────────────────────────────────────

# Create a bot via @BotFather on Telegram, then paste the token here.
# Format: 123456789:ABCdef...
# See DOCKER.md → Step 3 for full setup instructions.
TELEGRAM_BOT_TOKEN=

# ── Anthropic API key — powers the OpenClaw agent itself ──────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── CareerClaw Pro (optional) ─────────────────────────────────────────────────

# Your Anthropic or OpenAI API key for LLM-enhanced outreach drafts.
# Leave blank to run in free-tier mode (deterministic drafts only).
CAREERCLAW_LLM_KEY=

# Provider: "anthropic" (default) or "openai"
CAREERCLAW_LLM_PROVIDER=anthropic

# Model override — leave blank to use the provider default.
# Anthropic default: claude-sonnet-4-6
# OpenAI default:    gpt-4o-mini
CAREERCLAW_LLM_MODEL=
