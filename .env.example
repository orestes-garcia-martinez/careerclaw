# .env.example
#
# Copy this file to .env and fill in your values before running Docker.
# .env is gitignored — never commit your actual secrets.
#
#   cp .env.example .env
#   # then edit .env with your values


# ──────────────────────────────────────────────────────────────────────────────
# OpenClaw Gateway (Agent Layer)
# ──────────────────────────────────────────────────────────────────────────────

# Generated automatically by docker-setup.sh (official OpenClaw repo).
OPENCLAW_GATEWAY_TOKEN=

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=


# ──────────────────────────────────────────────────────────────────────────────
# Agent LLM Provider Keys (OpenClaw Model Layer)
# ──────────────────────────────────────────────────────────────────────────────
#
# The OpenClaw agent model is configured via:
#   openclaw-cli config set agents.defaults.model.primary <provider/model>
#
# Recommended default:
#   openai/gpt-5.2
#
# Provide at least ONE provider key below.

# Primary (recommended)
OPENAI_API_KEY=sk-proj-...

# Optional fallback provider
ANTHROPIC_API_KEY=sk-ant-...



# ──────────────────────────────────────────────────────────────────────────────
# CareerClaw Pro License
# ──────────────────────────────────────────────────────────────────────────────
#
# One-time license key (managed via Polar.sh).
# Leave blank to run in free-tier mode.
CAREERCLAW_PRO_KEY=



# ──────────────────────────────────────────────────────────────────────────────
# CareerClaw Pro LLM Enhancement (Draft Layer)
# ──────────────────────────────────────────────────────────────────────────────
#
# This controls the Pro-only draft enhancement feature.
# Separate from the OpenClaw agent model above.
#
# Enhancement runs only if:
#   - CAREERCLAW_PRO_KEY is valid
#   - An appropriate provider key is available
#   - Resume intelligence is present
#
# Provider-specific override keys (recommended for mixed failover chains)

# Optional explicit OpenAI key for draft enhancement
CAREERCLAW_OPENAI_KEY=

# Optional explicit Anthropic key for draft enhancement
CAREERCLAW_ANTHROPIC_KEY=

# Legacy single-key override (not recommended for mixed chains)
# If set, it will only be used when its format matches the provider.
CAREERCLAW_LLM_KEY=


# ──────────────────────────────────────────────────────────────────────────────
# Draft Enhancement Failover Configuration
# ──────────────────────────────────────────────────────────────────────────────

# Provider/model chain (order matters)
# Example:
#   openai/gpt-5.2,openai/gpt-4o-mini,anthropic/claude-sonnet-4-6
CAREERCLAW_LLM_CHAIN=openai/gpt-5.2,openai/gpt-4o-mini,anthropic/claude-sonnet-4-6

# Retry policy per candidate
CAREERCLAW_LLM_MAX_RETRIES=2

# Circuit breaker threshold (consecutive failures per run)
CAREERCLAW_LLM_CIRCUIT_BREAKER_FAILS=2